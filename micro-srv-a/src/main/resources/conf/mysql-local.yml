spring:
  datasource:
    mysql:
      dataopen:
        driverClassName: com.mysql.cj.jdbc.Driver
        jdbcUrl: jdbc:mysql://localhost:3306/mytest?serverTimezone=Asia/Shanghai
        username: root
        password: '123456'

mybatis:
  configuration:
    mysql:
      map-underscore-to-camel-case: on